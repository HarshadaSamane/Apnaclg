// Condition for Good String is starts with a and length greater than 3

let str = "apple";
if((str[0] === 'a') && (str.length > 3)) {
    console.log("This is a good string");
} else {
    console.log("This is not a good string");
}